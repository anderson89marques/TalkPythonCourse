{% extends "../shared/_layout.jinja2" %}

{% block content %}
<div class="content">
  <h1>Some Packages</h1>
  {% for p in packages %}
    <div>
      <span class="title">{{p.name}}</span>
      <span class="version">{{p.version}}</span>
    </div>
  {% endfor %}
  
</div>
{% endblock content %}
